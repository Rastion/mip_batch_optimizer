{
    "entry_point": "mip_batch_optimizer:MIPBatchOptimizer",
    "default_params": {
        "time_limit": 300
    },
    "optimizer_type": "MIP",
    "description": "Mixed Integer Programming based batch scheduling optimizer using Python-MIP and the CBC solver.",
    "compatible_problems": ["BatchScheduling"],
    "parameters": {
        "time_limit": {
            "type": "int",
            "description": "Maximum optimization time in seconds"
        }
    },
    "requirements": ["mip", "qubots"]
}
